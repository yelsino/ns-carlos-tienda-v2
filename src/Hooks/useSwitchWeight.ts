import { useEffect, useState } from 'react'

export const useSwitchWeight = () => {
//   const [disabled, setDisabled] = useState<boolean>(false)

//   useEffect(() => {
//     const timeout = setTimeout(() => {
//       setDisabled(false)
//       // solo se renderiza 1 vez
//     }, time)

//     return () => clearTimeout(timeout)
//   }, [disabled])

  return {} as const
}
